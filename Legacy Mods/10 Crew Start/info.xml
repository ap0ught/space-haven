<mod>
  <name>10CrewStart</name>
  <description>
  LEGACY PLACEHOLDER — imported from the Space Haven alpha modding community (2019).
  Originally set the starting crew count to 10 (instead of the default 5).
  Patch targets must be re-verified against 1.0 scenario/start data files.
  </description>
  <minimumLoaderVersion>0.10.0</minimumLoaderVersion>
  <version>0.0.1-legacy</version>
  <author>Unknown (legacy import)</author>
</mod>
